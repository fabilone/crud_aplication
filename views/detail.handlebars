<div class="div_body_content">
    <hr>
    <h5><i class="fas fa-angle-right"></i>&nbsp;<i class="fas fa-info-circle"></i>&nbsp;Detalhe da Aplicação:</h5>
    <br>
    <div class="card bg-light mb-3">
        {{#infoApp}}
        <div class="card-header"><b>{{name}}</b></div>
        <div class="card-body">
            <h5 class="card-title">Descrição:</h5>
            <p class="card-text">{{description}}</p>
        </div>
        <div class="card-footer bg-transparent border"><b>Linguagem:</b>  {{app_language}}  | <b>URL:</b> <a class="alert-link" href="{{URL}}">{{URL}}</a></div>
        {{/infoApp}}
    </div>
    <button class="btn btn-info" type="button" onClick="window.location='/list';" >Voltar</button>
</div>